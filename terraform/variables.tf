# Define any variables if needed in future
# Currently, everything is hardcoded for simplicity
variable "docker_image_name" {
  description = "Docker image name to deploy"
  default     = "major_project:latest"
}
